export class Category {
    constructor(
        id: number,
        count: number,
        link: string,
        slug: string,
        name: string,
        parentId: number,
        description?: string,
        meta?: []
    ) {}
}